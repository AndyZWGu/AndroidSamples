<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.andygu.sample_02">

  <!-- To auto-complete the email text field in the login form with the user's emails -->
  <uses-permission android:name="android.permission.GET_ACCOUNTS"/>
  <uses-permission android:name="android.permission.READ_PROFILE"/>
  <!-- 網路 -->
  <uses-permission android:name="android.permission.INTERNET"/>
  <!-- 寫入手機記憶卡的權限 -->
  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
  <!-- 讀取與寫入聯絡人 -->
  <uses-permission android:name="android.permission.READ_CONTACTS"/>
  <uses-permission android:name="android.permission.WRITE_CONTACTS"/>
<!--讀取取手機記憶卡的權限-->
  <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

  <application
      android:allowBackup="true"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:supportsRtl="true"
      android:theme="@style/AppTheme">
    <activity android:name=".activity.MainActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>

        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>
    <activity android:name=".activity.LoginActivity">
    </activity>
    <activity
        android:name=".activity.FinanceActivity"
        android:label="@string/title_activity_finance"
        android:theme="@style/AppTheme">
    </activity>
    <activity android:name=".activity.AddFinanceActivity">
    </activity>
    <activity android:name=".activity.UserProfileActivity">
    </activity>
    <activity android:name=".contactsContentActivity">
    </activity>
    <activity android:name=".activity.ContactsContentActivity">
    </activity>
    <activity android:name=".activity.PictureActivity">
    </activity>
  </application>

</manifest>